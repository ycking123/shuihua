<div align="center">
<img width="1200" height="475" alt="GHBanner" src="https://github.com/user-attachments/assets/0aa67016-6eaf-458a-adb2-6e31a0763ed6" />
</div>

# 水花项目

企业会议智能助手平台

## 📖 分形文档系统

本项目采用"分形文档 + 强制同构"的文档架构，确保代码与文档始终保持同步。

### 三层文档结构

1. **全局地图** (`MAP.md`) - 根目录
   - 项目整体架构
   - 模块间依赖关系
   - 快速定位表

2. **模块文档** (`各模块/MODULE.md`)
   - 模块职责说明
   - 成员清单（文件列表）
   - 接口说明

3. **文件头声明**（每个代码文件头部）
   - 依赖声明
   - 职责说明
   - 主要接口

### AI 上下文获取路径

> AI 从任意文件进入，三次 Read 即可获得完整上下文：
> 1. 读取当前文件头（依赖声明 + 职责说明）
> 2. 读取所在模块的 `MODULE.md`（成员清单 + 接口说明）
> 3. 读取根目录的 `MAP.md`（全局地图）

### 强制同构检查

**每次代码变更后必须运行**：

```bash
python scripts/check_docs_sync.py
```

该脚本会检查：
- ✅ 模块 `MODULE.md` 的成员清单是否完整
- ✅ 代码文件头部的依赖声明是否准确
- ✅ 文档与代码的一致性

## Run Locally

**Prerequisites:**  Node.js, Python


1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Run the app:
   `npm run dev`
